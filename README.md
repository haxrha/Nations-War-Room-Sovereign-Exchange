# SpacetimeDB — Portex Hackathon

Global Freight Exchange MVP: a live commodity-routing trading sim.

## Project

See **[portex/](portex/)** for the React frontend with mock simulation.

```bash
cd portex && npm install && npm run dev
```

Open http://localhost:5173
